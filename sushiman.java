import java.util.Scanner;

public class sushiman {
    public static void main(String[] args) {
        Scanner leia = new Scanner(System.in);
        
        int qntdCasas = leia.nextInt();
        String minNumeroStr = "1";
        String maxNumeroStr = "9";
        int i = 0;
        while (i < qntdCasas -1){
            minNumeroStr += "0";
            maxNumeroStr += "9";
        }        
        Integer minNumero = Integer.parseInt(minNumeroStr);
        Integer maxNumero = Integer.parseInt(maxNumeroStr);
        String res = "";
        while(minNumero <= maxNumero) {
            if
)        }
        
        
    }
    
    
}
